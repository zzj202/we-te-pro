
export interface UserInfo {
  phone: string
  createdAt: string
  lastLogin: string
  lastActivity: string
  loginCount: number
  // 可以添加更多字段如：
  // name?: string
  // avatar?: string
  // roles?: string[]
}